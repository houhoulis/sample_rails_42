<p id="notice"><%= notice %></p>

<h1>New User</h1>

<%= render 'form' %>

<% if @previous_user %>
  <p>
    This user might want to be friends with <b><%= @previous_user.name %></b>.
  </p>
<% end %>

<%= link_to 'Back', users_path %>
